apiVersion: apps/v1
kind: Deployment
metadata:
  name: event_bus-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: event_bus
  template:
    metadata:
      labels:
        app: event_bus
    spec:
      containers:
        - name: event_bus
          image: radebajic/event_bus
